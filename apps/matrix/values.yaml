elementweb:
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      external-dns.alpha.kubernetes.io/target: "homelab-tunnel.homelabm.xyz"
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    hosts:
      - host: &frontend_host chat.homelabm.xyz
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: element-tls-certificate
        hosts:
          - *frontend_host
  config:
    default:
      base_url: https://matrix.homelabm.xyz
      server_name: homelabm.xyz

dendrite:
  dendrite_config:
    global:
      server_name: matrix.homelabm.xyz
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostName: matrix.homelabm.xyz
    tls:
      - hosts:
          - matrix.homelabm.xyz
        secretName: matrix-tls-certificate
  postgresql:
    enabled: true
